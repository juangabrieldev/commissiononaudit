const rootUrl = 'http://localhost:4000';

export const endPoints = {
  registration: {
    employeeId: rootUrl + '/login/register/employeeid',
    username: rootUrl + '/login/register/username',
    register: rootUrl + '/login/register'
  }
};